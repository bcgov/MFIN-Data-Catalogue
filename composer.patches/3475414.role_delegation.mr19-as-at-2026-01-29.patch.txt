diff --git a/config/install/role_delegation.settings.yml b/config/install/role_delegation.settings.yml
new file mode 100644
index 0000000000000000000000000000000000000000..3da5bafc33194639b8033af9b8e90ed602c2ffae
--- /dev/null
+++ b/config/install/role_delegation.settings.yml
@@ -0,0 +1 @@
+show_roles_tab_administer_users: false
diff --git a/config/schema/role_delegation.schema.yml b/config/schema/role_delegation.schema.yml
new file mode 100644
index 0000000000000000000000000000000000000000..99e42f17725e49d027b53fe13a86c3af5a4ed064
--- /dev/null
+++ b/config/schema/role_delegation.schema.yml
@@ -0,0 +1,7 @@
+role_delegation.settings:
+  type: config_entity
+  label: 'Role delegation settings'
+  mapping:
+    show_roles_tab_administer_users:
+      type: boolean
+      label: 'Whether to show the Roles tab for users with the administer users permission.'
diff --git a/role_delegation.install b/role_delegation.install
new file mode 100644
index 0000000000000000000000000000000000000000..b097131603c9744aaf2da117d94edd1e6d0cd801
--- /dev/null
+++ b/role_delegation.install
@@ -0,0 +1,15 @@
+<?php
+
+/**
+ * @file
+ * Install hooks for role_delegation.
+ */
+
+/**
+ * Set show_roles_tab_administer_users.
+ */
+function role_delegation_update_10300(): void {
+  \Drupal::configFactory()->getEditable('role_delegation.settings')
+    ->set('show_roles_tab_administer_users', TRUE)
+    ->save();
+}
diff --git a/role_delegation.services.yml b/role_delegation.services.yml
index c9975108af6a68365f516b119a54eb6863c2dee2..d23def91b61320a340b708f8d62c644ea898caa4 100644
--- a/role_delegation.services.yml
+++ b/role_delegation.services.yml
@@ -1,12 +1,13 @@
 services:
+  _defaults:
+    autowire: true
   access_check.role_delegation:
     class: Drupal\role_delegation\Access\RoleDelegationAccessCheck
-    arguments: ['@delegatable_roles', '@permission_generator.role_delegation', '@current_user']
     tags:
       - { name: access_check, applies_to: _role_delegation_access_check }
   delegatable_roles:
     class: Drupal\role_delegation\DelegatableRoles
-    arguments: ['@module_handler']
+  Drupal\role_delegation\DelegatableRolesInterface: '@delegatable_roles'
   permission_generator.role_delegation:
     class: Drupal\role_delegation\PermissionGenerator
-    arguments: ['@delegatable_roles']
+  Drupal\role_delegation\PermissionGenerator: '@permission_generator.role_delegation'
diff --git a/src/Access/RoleDelegationAccessCheck.php b/src/Access/RoleDelegationAccessCheck.php
index c161ae7b85f1effaf4ab6372eb7d86170b949427..9eec3d854ed9561932df6deb1904fabf73adddfb 100644
--- a/src/Access/RoleDelegationAccessCheck.php
+++ b/src/Access/RoleDelegationAccessCheck.php
@@ -4,6 +4,7 @@ namespace Drupal\role_delegation\Access;
 
 use Drupal\Core\Access\AccessResult;
 use Drupal\Core\Access\AccessResultInterface;
+use Drupal\Core\Config\ConfigFactoryInterface;
 use Drupal\Core\Routing\Access\AccessInterface;
 use Drupal\Core\Session\AccountInterface;
 use Drupal\role_delegation\DelegatableRolesInterface;
@@ -14,41 +15,15 @@ use Drupal\role_delegation\PermissionGenerator;
  */
 class RoleDelegationAccessCheck implements AccessInterface {
 
-  /**
-   * The delegatable_roles service.
-   *
-   * @var \Drupal\role_delegation\DelegatableRolesInterface
-   */
-  protected $delegatableRoles;
-
-  /**
-   * The permission generator service.
-   *
-   * @var \Drupal\role_delegation\PermissionGenerator
-   */
-  protected $permissionGenerator;
-
-  /**
-   * The current user.
-   *
-   * @var \Drupal\Core\Session\AccountProxyInterface
-   */
-  protected $currentUser;
-
   /**
    * The Role Delegation access check.
-   *
-   * @param \Drupal\role_delegation\DelegatableRolesInterface $delegatable_roles
-   *   The delegatable_roles service.
-   * @param \Drupal\role_delegation\PermissionGenerator $permission_generator
-   *   The role delegation service.
-   * @param \Drupal\Core\Session\AccountInterface $current_user
-   *   The current user.
    */
-  public function __construct(DelegatableRolesInterface $delegatable_roles, PermissionGenerator $permission_generator, AccountInterface $current_user) {
-    $this->delegatableRoles = $delegatable_roles;
-    $this->permissionGenerator = $permission_generator;
-    $this->currentUser = $current_user;
+  public function __construct(
+    protected DelegatableRolesInterface $delegatableRoles,
+    protected PermissionGenerator $permissionGenerator,
+    protected AccountInterface $currentUser,
+    protected ConfigFactoryInterface $configFactory,
+  ) {
   }
 
   /**
@@ -66,17 +41,18 @@ class RoleDelegationAccessCheck implements AccessInterface {
     }
 
     // Deny access when the current user has the 'administer users'
-    // permission. Roles can be edited on the user edit page.
-    if ($account->hasPermission('administer users')) {
-      return AccessResult::neutral()->cachePerPermissions();
+    // permission unless configured. Roles can be edited on the user edit page.
+    $settings = $this->configFactory->get('role_delegation.settings');
+    if ($account->hasPermission('administer users') && !$settings->get('show_roles_tab_administer_users')) {
+      return AccessResult::neutral()->cachePerPermissions()->addCacheableDependency($settings);
     }
 
     // Deny access when the user is not allowed to assign any roles.
     if (!$this->delegatableRoles->getAssignableRoles($account)) {
-      return AccessResult::neutral()->cachePerPermissions();
+      return AccessResult::neutral()->cachePerPermissions()->addCacheableDependency($settings);
     }
 
-    return AccessResult::allowed()->cachePerPermissions();
+    return AccessResult::allowed()->cachePerPermissions()->addCacheableDependency($settings);
   }
 
 }
diff --git a/src/Plugin/Action/RoleDelegationManagerRoleUserTrait.php b/src/Plugin/Action/RoleDelegationManagerRoleUserTrait.php
index f7773be8629f7c11815a4c13096d056915bff9f0..f4559a124d5487f638cc56a1e93a3be6ae77941a 100644
--- a/src/Plugin/Action/RoleDelegationManagerRoleUserTrait.php
+++ b/src/Plugin/Action/RoleDelegationManagerRoleUserTrait.php
@@ -5,7 +5,6 @@ namespace Drupal\role_delegation\Plugin\Action;
 use Drupal\Core\Access\AccessResult;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\Core\Session\AccountInterface;
-use Drupal\user\Plugin\Action\AddRoleUser;
 use Symfony\Component\DependencyInjection\ContainerInterface;
 
 /**
diff --git a/tests/src/Functional/RoleAssignTest.php b/tests/src/Functional/RoleAssignTest.php
index 18f49304246294cba0fbad742fad6414024f6c3b..bbc8643f30fd5af3c406a6348da33f9de2ca7ff5 100644
--- a/tests/src/Functional/RoleAssignTest.php
+++ b/tests/src/Functional/RoleAssignTest.php
@@ -4,13 +4,14 @@ declare(strict_types=1);
 
 namespace Drupal\Tests\role_delegation\Functional;
 
+use Drupal\Core\Url;
 use Drupal\Tests\BrowserTestBase;
+use PHPUnit\Framework\Attributes\Group;
 
 /**
  * Functional tests for assigning roles.
- *
- * @group role_delegation
  */
+#[Group('role_delegation')]
 class RoleAssignTest extends BrowserTestBase {
 
   /**
@@ -81,7 +82,7 @@ class RoleAssignTest extends BrowserTestBase {
   /**
    * Test that we can assign roles we have access to via the Roles form.
    */
-  public function testRoleAssignRolesForm() {
+  public function testRoleAssignRolesForm(): void {
     $user_storage = \Drupal::entityTypeManager()->getStorage('user');
     // Create a role and login as a user with the permission to assign it.
     $rid1 = $this->drupalCreateRole([]);
@@ -123,7 +124,7 @@ class RoleAssignTest extends BrowserTestBase {
   /**
    * Test that we can assign roles we have access to via the user edit form.
    */
-  public function testRoleAssignUserForm() {
+  public function testRoleAssignUserForm(): void {
     $user_storage = \Drupal::entityTypeManager()->getStorage('user');
     $rid1 = $this->drupalCreateRole([]);
     $current_user = $this->drupalCreateUser([
@@ -162,46 +163,46 @@ class RoleAssignTest extends BrowserTestBase {
   /**
    * Test that the user has access to the role delegation page.
    */
-  public function testRoleDelegationPageAccess() {
+  public function testRoleDelegationPageAccess(): void {
     $regular_user = $this->drupalCreateUser();
 
     // Anonymous users can never access the roles page.
-    $this->drupalGet(sprintf('/user/%s/roles', $regular_user->id()));
-    $this->assertSession()->statusCodeEquals(403);
+    $rolesUrl = Url::fromRoute('role_delegation.edit_form', ['user' => $regular_user->id()]);
+    $this->assertFalse($rolesUrl->access());
 
     // Users with 'administer users' cannot view the page, they must use
     // the normal user edit page or also 'have assign all roles'.
-    $account = $this->createUser(['administer users']);
-    $this->drupalLogin($account);
-    $this->drupalGet(sprintf('/user/%s/roles', $regular_user->id()));
-    $this->assertSession()->statusCodeEquals(403);
+    $administerUsersUser = $this->createUser(['administer users']);
+    $this->assertFalse($rolesUrl->access($administerUsersUser));
+    \Drupal::configFactory()->getEditable('role_delegation.settings')
+      ->set('show_roles_tab_administer_users', TRUE)
+      ->save();
+    // User has administer users, but no role delegation permissions.
+    $this->assertFalse($rolesUrl->access($administerUsersUser));
+    // User with administer users AND role delegation perm is allowed access
+    // when config is enabled.
+    $this->assertTrue($rolesUrl->access($this->createUser([
+      'administer users',
+      'assign all roles',
+    ])));
 
     // Users with 'administer permissions' cannot view the page, they must use
     // the normal user edit page or also 'have assign all roles'.
-    $account = $this->createUser(['administer permissions']);
-    $this->drupalLogin($account);
-    $this->drupalGet(sprintf('/user/%s/roles', $regular_user->id()));
-    $this->assertSession()->statusCodeEquals(403);
+    $this->assertFalse($rolesUrl->access($this->createUser(['administer permissions'])));
 
     // Users with a custom 'assign %custom role' permission should be able to
     // see the role admin page.
     $role = $this->createRole([]);
-    $account = $this->createUser([sprintf('assign %s role', $role)]);
-    $this->drupalLogin($account);
-    $this->drupalGet(sprintf('/user/%s/roles', $regular_user->id()));
-    $this->assertSession()->statusCodeEquals(200);
+    $this->assertTrue($rolesUrl->access($this->createUser([sprintf('assign %s role', $role)])));
 
     // Users with 'assign all roles' can view the page.
-    $account = $this->createUser(['assign all roles']);
-    $this->drupalLogin($account);
-    $this->drupalGet(sprintf('/user/%s/roles', $regular_user->id()));
-    $this->assertSession()->statusCodeEquals(200);
+    $this->assertTrue($rolesUrl->access($this->createUser(['assign all roles'])));
   }
 
   /**
    * Test access to the "Roles" entity operation.
    */
-  public function testRoleDelegationEntityOperationAccess() {
+  public function testRoleDelegationEntityOperationAccess(): void {
     // Make sure the entity operation is only added to users.
     $node = $this->drupalCreateNode();
     $this->drupalGet('/admin/content');
diff --git a/tests/src/Functional/Views/RoleDelegationBulkOperationsTest.php b/tests/src/Functional/Views/RoleDelegationBulkOperationsTest.php
index 2e8d0de7d628be2ca19a7c20ab5c8bcede03cb1c..fd7b2eae6d69d97853be4c873775f4eb320bccc9 100644
--- a/tests/src/Functional/Views/RoleDelegationBulkOperationsTest.php
+++ b/tests/src/Functional/Views/RoleDelegationBulkOperationsTest.php
@@ -5,15 +5,15 @@ declare(strict_types=1);
 namespace Drupal\Tests\role_delegation\Functional\Views;
 
 use Drupal\Component\Render\FormattableMarkup;
-use Drupal\Tests\BrowserTestBase;
 use Drupal\system\Entity\Action;
+use Drupal\Tests\BrowserTestBase;
 use Drupal\views\Entity\View;
+use PHPUnit\Framework\Attributes\Group;
 
 /**
  * Functional tests for assigning roles in vbo.
- *
- * @group role_delegation
  */
+#[Group('role_delegation')]
 class RoleDelegationBulkOperationsTest extends BrowserTestBase {
 
   /**
diff --git a/tests/src/Kernel/AccessTest.php b/tests/src/Kernel/AccessTest.php
index 4d618f34a02fe37d9200a50bb81ab6bcf8037e5a..6d09b0a1d65716e5776d593304a661114b2527fb 100644
--- a/tests/src/Kernel/AccessTest.php
+++ b/tests/src/Kernel/AccessTest.php
@@ -5,14 +5,16 @@ declare(strict_types=1);
 namespace Drupal\Tests\role_delegation\Kernel;
 
 use Drupal\KernelTests\KernelTestBase;
-use Drupal\Tests\user\Traits\UserCreationTrait;
 use Drupal\role_delegation\Access\RoleDelegationAccessCheck;
+use Drupal\Tests\user\Traits\UserCreationTrait;
+use PHPUnit\Framework\Attributes\CoversClass;
+use PHPUnit\Framework\Attributes\Group;
 
 /**
- * @coversDefaultClass \Drupal\role_delegation\Access\RoleDelegationAccessCheck
- *
- * @group role_delegation
+ * Tests for the custom access check.
  */
+#[Group('role_delegation')]
+#[CoversClass(RoleDelegationAccessCheck::class)]
 class AccessTest extends KernelTestBase {
 
   use UserCreationTrait;
@@ -34,7 +36,6 @@ class AccessTest extends KernelTestBase {
    */
   protected function setUp(): void {
     parent::setUp();
-    $this->installSchema('system', 'sequences');
     $this->installEntitySchema('user');
     $this->accessChecker = $this->container->get('access_check.role_delegation');
 
@@ -45,8 +46,6 @@ class AccessTest extends KernelTestBase {
 
   /**
    * Test the access checker for user/%/roles.
-   *
-   * @covers ::access
    */
   public function testRoleDelegationAccess(): void {
     // Anonymous users can never access the roles page.
diff --git a/tests/src/Kernel/DelegatableRolesTest.php b/tests/src/Kernel/DelegatableRolesTest.php
index 75155d83cb7aa80201e3805a151739c53816a6c5..ed604321a3ccf2f75e500f37ee1c6c4b2eba8a3c 100644
--- a/tests/src/Kernel/DelegatableRolesTest.php
+++ b/tests/src/Kernel/DelegatableRolesTest.php
@@ -5,15 +5,18 @@ declare(strict_types=1);
 namespace Drupal\Tests\role_delegation\Kernel;
 
 use Drupal\KernelTests\KernelTestBase;
-use Drupal\Tests\user\Traits\UserCreationTrait;
+use Drupal\role_delegation\DelegatableRoles;
 use Drupal\role_delegation\DelegatableRolesInterface;
+use Drupal\Tests\user\Traits\UserCreationTrait;
 use Drupal\user\Entity\Role;
+use PHPUnit\Framework\Attributes\CoversClass;
+use PHPUnit\Framework\Attributes\Group;
 
 /**
- * @coversDefaultClass \Drupal\role_delegation\DelegatableRoles
- *
- * @group role_delegation
+ * Tests for the delegatable roles service.
  */
+#[Group('role_delegation')]
+#[CoversClass(DelegatableRoles::class)]
 class DelegatableRolesTest extends KernelTestBase {
 
   use UserCreationTrait;
@@ -35,7 +38,6 @@ class DelegatableRolesTest extends KernelTestBase {
    */
   protected function setUp(): void {
     parent::setUp();
-    $this->installSchema('system', 'sequences');
     $this->installEntitySchema('user');
     $this->delegatableRoles = $this->container->get('delegatable_roles');
 
@@ -46,8 +48,6 @@ class DelegatableRolesTest extends KernelTestBase {
 
   /**
    * Test the roles that can be assigned by a given user.
-   *
-   * @covers ::getAssignableRoles
    */
   public function testAssignableRoles(): void {
     $rid1 = $this->createRole([]);
@@ -71,8 +71,6 @@ class DelegatableRolesTest extends KernelTestBase {
 
   /**
    * Test the all roles methods filters special roles.
-   *
-   * @covers ::getAllRoles
    */
   public function testGetAllRoles(): void {
     $rid1 = $this->createRole([]);
