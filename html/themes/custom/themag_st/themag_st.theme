<?php

/**
 * @file
 * Template for themag.template.
 */

// Include files with preprocess functions.
foreach (glob(dirname(__FILE__) . '/includes/*.inc') as $filename) {
  require_once dirname(__FILE__) . '/includes/' . basename($filename);
}
